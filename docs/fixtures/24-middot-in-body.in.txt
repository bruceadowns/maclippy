⏺ Yes — §0 says "copied from a terminal session," but fixtures 1, 4 and 5 are classified authored regime, described in §6.1 as "prose that approximates a column," and breakTolerance = 8 is justified as
  absorbing "authored-prose drift."

  I think that's a wording problem rather than a domain problem, and it needs deciding either way. A Claude response full of short bullets and headers produces lines that never reach the wrap column, so the
  estimator sees the same shape as hand-typed prose — those pastes are genuinely in domain. But the spec's language says a human typed them, which §0 now forbids. The regime distinction is really about
  whether lines reached the column, not about who wrote them, and a constant is currently justified by a cause the domain excludes. Left as-is, the next reader concludes either that §0 is wrong or that three
  fixtures should be deleted.

  The third gap is the one I'd worry about most: §0's destination constraint has no evidence behind it. I wrote that most destinations don't render Markdown, and derived two decisions from it — deleting · →
  *, closing the fence item as won't-do. Every other claim in that spec is backed by a measurement. This one is backed by me reasoning about Jira and Teams. The evidence isn't a paste; it's you reformatting a
  stanza, dropping it into an actual ticket and an actual Teams message, and reporting what the table, the ▎ block and the embedded code block actually looked like. That would either confirm the newest
  section or correct it — and it would settle whether §5.1's box-table → Markdown conversion is still right, which I flagged as needing re-derivation and then didn't touch.

  The one place another sample would help: a second prose-with-embedded-code paste. §6.1 records that rule as resting on one sample with the ;{} majority threshold uncalibrated, and it's the thinnest thing
  left standing. But that's worth waiting for one to occur naturally rather than going looking.
